//
//  Brand.swift
//  AudiopigShared
//

import Foundation

/// User-facing product name. Technical identifiers (bundle ID, targets, URL scheme) stay `Audiopig`.
public enum Brand {
    public static let displayName = "AudioPig"
    public static let plusName = "AudioPig Plus"
}
